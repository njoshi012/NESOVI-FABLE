{% comment %}
  DEPRECATED — this block was never actually wired into the cart drawer (the drawer is a snippet,
  not a section, so it never had a slot to accept it as a block). Replaced by
  snippets/nesovi-cart-upsell.liquid, rendered directly from snippets/cart-drawer.liquid and
  configured via Theme settings > NESOVI Cart Upsell (product/collection pickers, source, limit).
  Safe to delete from Admin > Edit code. No preset = not addable in editor.
{% endcomment %}
{% schema %}
{ "name": "zz Deprecated", "settings": [] }
{% endschema %}
